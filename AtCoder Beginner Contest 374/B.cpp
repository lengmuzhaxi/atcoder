/*
#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s,t;
    cin>>s>>t;
    for(int i=0;i<min(s.size(),t.size());i++)
    {
        if(s[i]!=t[i])
        {
            cout<<i+1;
            return 0;
        }
    }
    if(s.size()==t.size())
        cout<<'0'<<endl;
    else
        cout<<min(s.size(),t.size())+1<<endl;
}
*/
